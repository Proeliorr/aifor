diff --git a/PointCloudSegmentation/conf/models/panoptic/FORpartseg_3heads.yaml b/PointCloudSegmentation/conf/models/panoptic/FORpartseg_3heads.yaml
index fa9002b..84d40b5 100644
--- a/PointCloudSegmentation/conf/models/panoptic/FORpartseg_3heads.yaml
+++ b/PointCloudSegmentation/conf/models/panoptic/FORpartseg_3heads.yaml
@@ -174,4 +174,15 @@ PointGroup-PAPER:
   cluster_type: 7
   vizual_ratio: 0
 
-  path_pretrained: "/cluster/work/igp_psr/binbin/OutdoorPanopticSeg_V2/outputs/setting1_classes5_mixtree_1000/setting1_classes5_mixtree_1000-PointGroup-PAPER-20240205_132553/PointGroup-PAPER.pt"
+  # LOCAL PATCH (SegmentedForests): was a FIVE-class checkpoint on the original author's
+  # cluster ("setting1_classes5_mixtree_1000/...PointGroup-PAPER.pt"). Two reasons it is
+  # null now:
+  #   1. The path does not exist here, so it only ever produced
+  #      "The path does not exist, it will not load any model" (base_model.py:154) --
+  #      every run was already training from scratch, by accident rather than by choice.
+  #   2. If it ever DID resolve, base_model.py calls
+  #      load_state_dict_with_same_shape(m, strict=False), which silently skips layers
+  #      whose shape changed. Our semantic head is 4-wide, that checkpoint's is 5, so the
+  #      head would be quietly dropped and you would believe you were fine-tuning.
+  # Point this at a 4-class checkpoint to fine-tune.
+  path_pretrained: null
diff --git a/PointCloudSegmentation/conf/training/default.yaml b/PointCloudSegmentation/conf/training/default.yaml
index e07724b..7af3899 100644
--- a/PointCloudSegmentation/conf/training/default.yaml
+++ b/PointCloudSegmentation/conf/training/default.yaml
@@ -1,8 +1,8 @@
 # @package training
 # Those arguments defines the training hyper-parameters
-epochs: 100
-num_workers: 6
-batch_size: 16
+epochs: 5
+num_workers: 0
+batch_size: 4
 shuffle: True
 cuda: 0 # -1 -> no cuda otherwise takes the specified index
 precompute_multi_scale: False # Compute multiscate features on cpu for faster training / inference
@@ -29,15 +29,16 @@ checkpoint_dir: ""
 # Those arguments within experiment defines which model, dataset and task to be created for benchmarking
 # parameters for Weights and Biases
 wandb:
-  entity: ""
-  project: default
+  entity: "aifor" #TO ADAPT: change to your own wandb account name
+  project: FOR-dataset #panoptic-boost
   log: True
-  notes:
-  name:
+  notes: ""
+  name: "aifor" #TO ADAPT: specify name of experiment that will be shown on wandb
+  id:
   public: True # It will be display the model within wandb log, else not.
   config:
-    model_name: ${model_name}
+    grid_size: ${data.grid_size}
 
   # parameters for TensorBoard Visualization
 tensorboard:
-  log: True
+  log: False
diff --git a/PointCloudSegmentation/conf/training/treeins_set1.yaml b/PointCloudSegmentation/conf/training/treeins_set1.yaml
index 54b0608..0788b3f 100644
--- a/PointCloudSegmentation/conf/training/treeins_set1.yaml
+++ b/PointCloudSegmentation/conf/training/treeins_set1.yaml
@@ -30,11 +30,11 @@ checkpoint_dir: ""
 # Those arguments within experiment defines which model, dataset and task to be created for benchmarking
 # parameters for Weights and Biases
 wandb:
-  entity: "binbin" #TO ADAPT: change to your own wandb account name
+  entity: "aifor" #TO ADAPT: change to your own wandb team/account name
   project: FOR-dataset #panoptic-boost
   log: True
   notes: "set1_5classes treeins"
-  name: "set1_5classes treeins" #TO ADAPT: specify name of experiment that will be shown on wandb
+  name: "aifor" #TO ADAPT: specify name of experiment that will be shown on wandb
   id:
   public: True # It will be display the model within wandb log, else not.
   config:
diff --git a/PointCloudSegmentation/torch_points3d/datasets/panoptic/treeins_set1.py b/PointCloudSegmentation/torch_points3d/datasets/panoptic/treeins_set1.py
index 4fe0472..fa659cb 100644
--- a/PointCloudSegmentation/torch_points3d/datasets/panoptic/treeins_set1.py
+++ b/PointCloudSegmentation/torch_points3d/datasets/panoptic/treeins_set1.py
@@ -18,12 +18,14 @@ from torch_points3d.models.panoptic.ply import read_ply, write_ply
 
 # @Treeins
 # -1 means unlabelled semantic classes
+# LOCAL PATCH (SegmentedForests): 5 -> 4 classes; upstream class 4 "branches" has no
+# counterpart in the SegmentedForests labelling. Kept in sync with the same names and
+# colours in datasets/segmentation/treeins_set1.py, which declares them a second time.
 CLASSES_INV = {
     0: "low_vegetation",
     1: "ground",
     2: "stem_points",
     3: "live_branches",
-    4: "branches",
 }
 
 OBJECT_COLOR = np.asarray(
@@ -32,13 +34,14 @@ OBJECT_COLOR = np.asarray(
         [95, 156, 196],  # 'ground' .-> . blue
         [179, 116, 81],  # 'stem_points'  ->  brown
         [241, 149, 131],  # 'live_branches'  ->  salmon
-        [81, 163, 148],  # 'branches'  ->  bluegreen
         [0, 0, 0],  # unlabelled .->. black
     ]
 )
 
-VALID_CLASS_IDS = [0, 1, 2, 3, 4]
-SemIDforInstance = np.array([2,3,4])  # only class tree/1 is is a semantic class with instances, namely tree instances. In contrast, semantic class non-tree is a stuff class.
+VALID_CLASS_IDS = [0, 1, 2, 3]
+# Semantic classes that carry tree instances. "branches" (4) is gone, so instances now
+# come from stem_points + live_branches only.
+SemIDforInstance = np.array([2,3])  # only class tree/1 is is a semantic class with instances, namely tree instances. In contrast, semantic class non-tree is a stuff class.
 
 
 ################################### UTILS #######################################
@@ -102,20 +105,31 @@ def to_ins_ply(pos, label, file):
 
 # @Treeins: added parameter output_file_name because we now save several final evaluation files because we have several test files
 def final_eval(pre_sem, pre_ins_offset, pos, gt_sem, gt_ins, output_file_name):
+    # LOCAL PATCH (SegmentedForests): 5 -> 4 semantic classes.
+    # NOTE the counters below are 1-BASED and shifted: 0 = unclassified, so semantic
+    # class i is counted as i+1. Dropping 0-based class 4 ("branches") therefore drops
+    # the 1-based id 5 from the lists.
+    # NUM_CLASSES stays 3: it counts the BINARY scheme (unclassified / stuff / thing)
+    # used further down, after the labels are collapsed at "mapping labels", and is
+    # independent of how many fine-grained classes exist.
     NUM_CLASSES = 3  #@Treeins: classes unclassified, non-tree and tree
-    NUM_CLASSES_sem = 6
-    NUM_CLASSES_count = 5  # @Treeins: 2 classes without unclassified
+    NUM_CLASSES_sem = 5
+    # Unused in THIS copy of final_eval (mIoU below divides by len(sem_classcount_final)
+    # instead), but the sibling copies do `sum(iou_list) / NUM_CLASSES_count` -- see
+    # treeins.py:187 and npm3d_4class.py:186. Keeping a stale 5 here would silently
+    # divide the headline metric by the wrong number the moment anyone copies that line.
+    NUM_CLASSES_count = 4  # @Treeins: semantic classes excluding unclassified
     # class index for instance segmenatation
     ins_classcount = [2]  # @Treeins
     # class index for stuff segmentation
     stuff_classcount = [1]  # @Treeins
     # class index for semantic segmenatation
-    sem_classcount = [1, 2, 3, 4, 5]  # @Treeins
-    sem_classcount_remove_ground = [1, 3, 4, 5]
+    sem_classcount = [1, 2, 3, 4]  # @Treeins
+    sem_classcount_remove_ground = [1, 3, 4]
     #class index for semantic classes with gt points
     sem_classcount_have = []
     stuff_classes = [1,2]
-    thing_classes = [3,4,5]
+    thing_classes = [3,4]
 
     # log directory
     # file_path = '/scratch2/torch-points3d/outputs/2021-10-20/06-19-43/eval/2021-10-26_14-27-55/'
diff --git a/PointCloudSegmentation/torch_points3d/datasets/segmentation/treeins_set1.py b/PointCloudSegmentation/torch_points3d/datasets/segmentation/treeins_set1.py
index da8e53f..5242246 100644
--- a/PointCloudSegmentation/torch_points3d/datasets/segmentation/treeins_set1.py
+++ b/PointCloudSegmentation/torch_points3d/datasets/segmentation/treeins_set1.py
@@ -32,13 +32,15 @@ log = logging.getLogger(__name__)
 
 
 #@Treeins: two semantic segmentation classes: non-tree and tree
-Treeins_NUM_CLASSES = 5
+# LOCAL PATCH (SegmentedForests): 5 -> 4 classes. Upstream class 4 "branches" has no
+# counterpart in the SegmentedForests labelling, so nothing ever maps to it; leaving it
+# declared would train a head for a class that cannot occur. See patches/README.md.
+Treeins_NUM_CLASSES = 4
 INV_OBJECT_LABEL = {
     0: "low_vegetation",
     1: "ground",
     2: "stem_points",
     3: "live_branches",
-    4: "branches",
 }
 
 OBJECT_COLOR = np.asarray(
@@ -47,7 +49,6 @@ OBJECT_COLOR = np.asarray(
         [95, 156, 196],  # 'ground' .-> . blue
         [179, 116, 81],  # 'stem_points'  ->  brown
         [241, 149, 131],  # 'live_branches'  ->  salmon
-        [81, 163, 148],  # 'branches'  ->  bluegreen
         [0, 0, 0],  # unlabelled .->. black
     ]
 )
diff --git a/PointCloudSegmentation/train.py b/PointCloudSegmentation/train.py
index c7cbd87..a0c1da2 100644
--- a/PointCloudSegmentation/train.py
+++ b/PointCloudSegmentation/train.py
@@ -3,17 +3,22 @@ from hydra.core.global_hydra import GlobalHydra
 from omegaconf import OmegaConf
 from torch_points3d.trainer import Trainer
 import logging
+import debugpy
 
 @hydra.main(config_path="conf", config_name="config")
 def main(cfg):
+    # debugpy.breakpoint()   # add — process-initiated stop, ignores pathMappings
+
     
     numba_logger = logging.getLogger('numba')
     numba_logger.setLevel(logging.WARNING)
     OmegaConf.set_struct(cfg, False)  # This allows getattr and hasattr methods to function correctly
     if cfg.pretty_print:
         print(OmegaConf.to_yaml(cfg))
-
+    
     trainer = Trainer(cfg)
+    print(Trainer)
+    # debugpy.breakpoint()   # add — process-initiated stop, ignores pathMappings
     trainer.train()
     #
     # # https://github.com/facebookresearch/hydra/issues/440
