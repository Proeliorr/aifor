# Already satisfied by the `aifor` mamba env — listed for reproducibility.
hydra-zen==0.16.0
hydra-core==1.3.2
omegaconf==2.3.0
laspy==2.5.4
lazrs==0.8.1
plyfile==1.1
PyYAML==6.0.3
# Diagnostic tooling only (misc/view_split_point_cloud.ipynb), not needed by the
# pipeline stages themselves. pip-installed with --user: the conda pkg cache in
# C:\ProgramData is not writable in this env.
matplotlib==3.11.0
pyvista==0.48.4               # 3-D view (misc/view_cloud_3d.py)
vtk==9.6.2                    # pyvista's rendering backend
# NB: Open3D is deliberately NOT used — it ships no wheels for this env's
# Python 3.13, so PyVista+VTK provide the orbitable 3-D viewport instead.
# External CLI tool (not a pip dependency of this package):
#   3DFin==0.6.0  — installed in the aifor env, invoked via `threedfin_bin`
